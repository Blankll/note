# cluster

MySQL benchmark

```bash
mysqlslap -h -u -p -P 3306 --concurrency=5000 --iteraions=1 --auto-generate-sql \
--auto-generate-sql-load-type=mixed --auto-generate-sql-add-autoincrement\
--engine=innodb --number-of-queries=5000 --debug-info
```

```bash
mysql -u villager -p 0707 --concurrency=5000 --iterations=1 --auto-generate-sql --auto-generate-sql-load-type=mixed --auto-generate-sql-qutoincrement --engine=innodb --numberof-queries=5000 --debug-info
```



# 发斯蒂芬

的房间都是